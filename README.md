# Portal Prototype
A small 3D rendering project made with **C#/.NET, OpenTK, OpenGL 3.3 and GSLS 330**.

## Run

Requires a recent .NET SDK and a graphics driver with OpenGL 3.3 support.

```bash
dotnet restore
dotnet build
dotnet run
